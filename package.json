{
  "name": "privacy-mirror",
  "version": "0.1.0",
  "private": true,
  "description": "Cross-browser, evidence-driven privacy instrumentation and minimum-intervention protection.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "node --test tests/*.test.cjs",
    "check": "npm run lint && npm test && npm run build",
    "server": "node apps/analysis-server/server.cjs",
    "dashboard": "node scripts/static-server.mjs apps/dashboard 4173"
  },
  "engines": {
    "node": ">=20"
  }
}
